# Repository instructions

- Keep this site framework-free and directly publishable by GitHub Pages.
- Use plain HTML, CSS, and minimal vanilla JavaScript only.
- Do not add a package manager, generated build output, server runtime, Jekyll configuration, or deployment workflow.
- Preserve the SEO metadata, structured data, `robots.txt`, `sitemap.xml`, and App Store ID `6793854994` when editing.
- Verify desktop and mobile rendering after visual changes.
